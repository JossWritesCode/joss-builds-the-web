# Joss Builds the Web — Portfolio

Production-ready Vite + React + TypeScript + Tailwind scaffold with React Router, Framer Motion, lucide-react, and SEO.

## Quick start

```bash
npm i
cp .env.example .env
npm run dev

```
